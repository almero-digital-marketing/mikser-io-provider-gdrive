{
  "name": "mikser-io-provider-gdrive",
  "version": "1.1.0",
  "description": "Google Drive as a content source for mikser-io. Lifecycle plugin syncs Drive folders into the catalog as entities; `read(entity)` named export plugs into the engine's scheme-dispatched readEntityContent so Docs / Sheets / Slides / plain text / binaries all just work downstream.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "node --no-warnings --test --test-reporter=spec 'test/**/*.test.js'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/almero-digital-marketing/mikser-io-provider-gdrive.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/almero-digital-marketing/mikser-io-provider-gdrive/issues"
  },
  "homepage": "https://github.com/almero-digital-marketing/mikser-io-provider-gdrive#readme",
  "peerDependencies": {
    "mikser-io": "^9.0.0"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "minimatch": "^10.0.0"
  }
}
